<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="com.gsm.presentation.ui.chat.ChatModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:id="@+id/chat_card_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        app:cardCornerRadius="4dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:padding="10dp"
            android:background="@drawable/background_practice_test"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/chat_tv_nickname"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="20dp"
                android:layout_marginBottom="10dp"
                android:fontFamily="@font/noto_sans_kr_bold"
                android:includeFontPadding="false"
                android:padding="10dp"
                android:text="@{data.nickname}"
                android:textColor="?android:attr/textColorPrimary"
                android:textSize="30dp"
                android:textStyle="bold"
                app:layout_constraintBottom_toBottomOf="@+id/chat_tv_time"
                app:layout_constraintStart_toEndOf="@+id/chat_tv_time"
                app:layout_constraintTop_toTopOf="@+id/chat_tv_time"
                app:layout_constraintVertical_bias="1.0"
                tools:text="보석이" />

            <TextView
                android:id="@+id/chat_tv_contents"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:background="@drawable/background_practice_test"
                android:elevation="5dp"
                android:padding="10dp"
                android:text="@{data.contents}"
                android:textColor="?android:attr/textColorPrimary"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.0"
                app:layout_constraintStart_toStartOf="parent"
                tools:text="저는 경민이랑 섹스하고싶은 마은이 있어요 외로워요 연락즈쉐" />

            <ImageView
                android:id="@+id/chat_tv_time"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_marginBottom="20dp"
                android:elevation="4dp"
                android:padding="10dp"
                android:scaleType="centerCrop"
                android:src="@drawable/curby"
                android:textSize="12sp"
                app:chatImage="@{data.profile_image}"
                app:layout_constraintBottom_toTopOf="@+id/chat_tv_contents"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="작성시간" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>